import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const primary = Color(0xFF199A8E);
  static const secondary = Color(0xFF082659);

  // Background
  static const background = Color(0xFFF8F9FA);
  static const white = Color(0xFFFFFFFF);

  // Text
  static const textPrimary = Color(0xFF101623);
  static const textSecondary = Color.fromARGB(255, 20, 90, 83);
  static const textLight = Color(0xFFA1A8B9);

  // Accent
  static const accent = Color(0xFF199A8E);
  static const success = Color(0xFF0EBE7F);
  static const error = Color(0xFFFF4B4B);
}
